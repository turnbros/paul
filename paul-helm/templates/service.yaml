apiVersion: v1
kind: Service
metadata:
  name: {{ include "paul.fullname" . }}
  labels:
    {{- include "paul.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paul.selectorLabels" . | nindent 4 }}
