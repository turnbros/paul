apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paul.fullname" . }}-test-connection"
  labels:
    {{- include "paul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
